# Allow legacy peer dependencies for React 19 compatibility
legacy-peer-deps=true

# Auto install peer dependencies
auto-install-peers=true

# Prefer offline packages
prefer-offline=true

# Strict peer dependencies disabled for React 19 migration
strict-peer-dependencies=false 